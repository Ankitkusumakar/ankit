<%= form_with(model: ankit, local: true) do |form| %>
  <% if ankit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ankit.errors.count, "error") %> prohibited this ankit from being saved:</h2>

      <ul>
        <% ankit.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :age %>
    <%= form.text_field :age %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
